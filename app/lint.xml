<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- AAPT2 requires adaptive launcher icons in a -v26 directory even though minSdk is 26. -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/res/mipmap-anydpi-v26" />
    </issue>
    <!-- compileSdk/targetSdk are pinned to the SDK level that is installed and tested; bump them deliberately in libs.versions.toml. -->
    <issue id="OldTargetApi" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />
</lint>
